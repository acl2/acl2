; We need the following for provisional certification, so that ACL2
; can read symbols flag::SYM in too-many-ifs.acl2x at the start of
; the Pcertify step.

(in-package "ACL2")
(include-book "tools/flag" :dir :system)
(certify-book "too-many-ifs" ? t)
