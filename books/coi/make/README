## You should either source this file or make it part of
## your .cshrc/.bashrc with appropriate modifications.

## ${acl2-3.1} is the directory containing the ACL2 3.1 source
## ${installation} is the directory containing this "make" directory
##                 and the other RCI libraries
## ${javapath} points to a Java installation
## ${XalanLibPath} locates a lib directory containing Xalan .so files
## ${XercesLibPath} locates a lib directory containing Xerces .so files

## The following ENVIORNMENT variables must be defined:

## The system builds with acl2-3.1
setenv ACL2 ${acl2-3.1}/saved_acl2
setenv ACL2_SYSTEM_BOOKS = ${acl2-3.1}/books

## Point to the vFaat CLI Directory
setenv VFAAT_ROOT ${installation}/vfaatCLI-0.1.5
setenv VFAAT_XSL  $VFAAT_ROOT/plugins/com.rockwellcollins.vfaat.external_1.0.0/macro

## vFaat requires Java 
setenv JAVA_HOME ${javapath}/jdk1.5.0_06
set path = (${path} ${javapath}/jdk1.5.0_06/bin)

## vFaat requires Xalan and Xerces (from Apache)
setenv LD_LIBRARY_PATH ${LD_LIBRARY_PATH}:${XalanLibPath}/lib:${XercesLibPath}/lib

## RAMS_MAKEDIR should point to this (the make) directory
setenv RAMS_MAKEDIR ${installation}/make

## To make the whole distribution, from $(RAMS_MAKEDIR) type:
## make -f Makelibs
