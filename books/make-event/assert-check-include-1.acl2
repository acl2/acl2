(in-package "ACL2")
(include-book "../misc/eval")

; Commented out because setenv$ causes an error in sbcl and besides,
; it's not clear why this is needed.
; (setenv$ "ACL2_SAVE_EXPANSION" "t")

(certify-book "assert-check-include-1" ? t)
