/*
 * generated by Xtext 2.26.0
 */
package org.neu.acl2.handproof.web;

import com.google.inject.Guice;
import com.google.inject.Injector;
import org.eclipse.xtext.util.Modules2;
import org.neu.acl2.handproof.HandProofRuntimeModule;
import org.neu.acl2.handproof.HandProofStandaloneSetup;
import org.neu.acl2.handproof.ide.HandProofIdeModule;

/**
 * Initialization support for running Xtext languages in web applications.
 */
public class HandProofWebSetup extends HandProofStandaloneSetup {
	
	@Override
	public Injector createInjector() {
		return Guice.createInjector(Modules2.mixin(new HandProofRuntimeModule(), new HandProofIdeModule(), new HandProofWebModule()));
	}
	
}
