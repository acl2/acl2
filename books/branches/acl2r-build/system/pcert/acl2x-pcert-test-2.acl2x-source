
((3 RECORD-EXPANSION
    (ENCAPSULATE NIL
		 (LOCAL (MAKE-EVENT '(DEFUN G2 (X) X)))
		 (DEFUN H2 (X) X))
    (ENCAPSULATE NIL
		 (RECORD-EXPANSION
		  (LOCAL (MAKE-EVENT '(DEFUN G2 (X) X)))
		  (LOCAL (DEFUN G2 (X) X)))
		 (DEFUN H2 (X) X)))
 (4 RECORD-EXPANSION
    (ENCAPSULATE NIL
		 (LOCAL (MAKE-EVENT '(DEFUN G3 (X) X)))
		 (DEFUN H3 (X) X))
    (ENCAPSULATE NIL
		 (RECORD-EXPANSION
		  (LOCAL (MAKE-EVENT '(DEFUN G3 (X) X)))
		  (LOCAL (value-triple :elided)))
		 (DEFUN H3 (X) X))))
